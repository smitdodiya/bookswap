{
  "name": "bookswap",
  "private": true,
  "version": "1.0.0",
  "description": "BookSwap — peer-to-peer book exchange (React + Express + SQLite/Prisma)",
  "scripts": {
    "install:all": "cd server && npm install && cd ../client && npm install",
    "seed": "cd server && npm run seed",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "dev": "concurrently -n server,client -c yellow,cyan \"npm:dev:server\" \"npm:dev:client\"",
    "build": "cd server && npm install --include=dev && npx prisma generate && cd ../client && npm install --include=dev && npm run build",
    "start": "cd server && npx prisma db push --skip-generate && node src/index.js"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
